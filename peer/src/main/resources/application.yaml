spring:
  application:
    name: peer
  grpc:
    server:
      port: ${peer.port:6001}

tracker:
  address: localhost
  port: 50051

peer:
  advertise-address: 127.0.0.1

# Benchmark / CI: set true to skip noisy stdout (or use -Dcds.distdownloader.quiet=true / --cds.distdownloader.quiet=true)
cds:
  distdownloader:
    quiet: false
