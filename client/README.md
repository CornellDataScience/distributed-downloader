# Client Scaffold

This directory now contains minimal starter files only.

Included:
- `Client.java`: entrypoint with default args (`127.0.0.1`, `50051`, `env/manifest.json`)
- `ClientService.java`: placeholder service with TODO output
- `pom.xml`: minimal Maven module descriptor

Intentionally not implemented:
- tracker RPC flow
- peer availability/chunk fetch
- chunk assembly / hash validation
